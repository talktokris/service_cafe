# cPanel SSH deploy config
# Copy this file to deploy-config.env and fill in your details.
# Do not commit deploy-config.env (it should be in .gitignore).

# cPanel SSH host (e.g. your domain or server hostname)
SSH_HOST=your-server.com

# cPanel SSH username (usually your cPanel account name)
SSH_USER=your_cpanel_username

# Remote path to the project root on the server (e.g. public_html/servecafe or just public_html)
REMOTE_PATH=repositories/service_cafe

# SSH port (default 22; cPanel sometimes uses 21098 or another port)
SSH_PORT=22

# Optional: path to your private key file (leave empty to use password)
# SSH_KEY=~/.ssh/id_rsa
